/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ScoringRule } from '../../models/scoring-rule';

export interface UpdateScoringRules$Params {
  id: number;
      body: ScoringRule
}

export function updateScoringRules(http: HttpClient, rootUrl: string, params: UpdateScoringRules$Params, context?: HttpContext): Observable<StrictHttpResponse<ScoringRule>> {
  const rb = new RequestBuilder(rootUrl, updateScoringRules.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ScoringRule>;
    })
  );
}

updateScoringRules.PATH = '/competitions/{id}/scoring-rules';
